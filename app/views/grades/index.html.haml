%fieldset.control-group
  %legend All Subjects
  - if @grades.count > 0
    %table.table.table-stripped
      %thead
        %tr
          %th Grade
          %th Quarter
          %th
          %th
          %th
      %tbody
      - @grades.each do |grade|
        %tr
          %td= grade.grade
          %td= grade.quarter
          %td= link_to 'Show', grade
          %td= link_to 'Edit', edit_grade_path(grade)
          %td= link_to 'Destroy', grade, :confirm => 'Are you sure?', :method => :delete

  - else
    .well
      %p.text-center= t :no_data
  .form-actions
    = link_to 'New Grades', new_grade_path, :class => 'btn btn-primary'
